module testing.a
{ext|PostfixOperators, PackageImports}
infixl 6 +
a + b = {ffi|(+)}
infixl 7 /
a / b = {ffi|(div)}
postfix %
(%) a = a / 100

x :: Float
x = 100% + 50%

y :: forall a. a -> a
y = fix lam x -> 42

seq :: a -> b -> b
a `seq` b = b